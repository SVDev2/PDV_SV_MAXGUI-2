function conferLog() {
    let user = document.getElementById("user").value;
    let senha = document.getElementById("senha").value;
    let loja = document.getElementById("loja").value;

    if (user == "" || senha == "" || loja == "") { document.getElementById("resposta").innerHTML = "Todos os campos devem ser preenchidos";}
    else { };
};

function creatorLog() {};

function tipoLog() {};